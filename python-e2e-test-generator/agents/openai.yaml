interface:
  display_name: "Python E2E Test Generator"
  short_description: "Generate source-tracked pytest business E2E scenarios"
  default_prompt: "Use $python-e2e-test-generator to generate or incrementally update self-contained Chinese business scenarios with per-scenario source-version impact checks and collectable pytest code."
